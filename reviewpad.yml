checks:
  non-conventional-commits:
    activation: on
  large:
    parameters:
      max-lines: -1
  stale:
    parameters:
      max-idle-days: -1
  high-code-churn:
    parameters:
      max-rate: -1
      min-deleted-lines: -1
     
workflows:
  - name: test
    run: $addLabel("test")
