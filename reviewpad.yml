checks:
  non-conventional-commits:
    activation: on

workflows:
  - name: test1
    run:
      if: $size() > 1
      then: $addLabel("more than 1")

  - name: test2
    run:
      if: $hasLabel("more than 1")
      then: $addLabel("has more than 1 label")

  - name: test3
    run:
      if: $containsFileName("test")
      then: $addLabel("test")
